sequenceDiagram
    participant User
    participant CLI
    participant Incremental as Incremental Engine
    participant Stream as Progressive Stream

    User->>CLI: repo-explainer update repo --incremental
    CLI->>Incremental: request diff plan
    Incremental-->>CLI: list impacted modules
    CLI->>Stream: tail progressive docs/logs
    Stream-->>User: updates + cost alerts
    CLI-->>User: summary of refreshed sections
